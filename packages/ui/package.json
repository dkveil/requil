{
	"name": "@requil/ui",
	"version": "0.0.0",
	"private": true,
	"exports": {
		"./*": "./src/*.tsx"
	},
	"scripts": {
		"build": "echo 'UI package - no build needed' && exit 0",
		"lint": "biome lint .",
		"format": "biome format --write .",
		"check": "biome check --write .",
		"generate:component": "turbo gen react-component",
		"type-check": "echo 'UI package - type check skipped for now' && exit 0",
		"test": "echo 'No tests yet' && exit 0"
	},
	"devDependencies": {
		"@requil/config": "workspace:*",
		"@types/node": "^24.10.0",
		"@types/react": "19.1.0",
		"@types/react-dom": "19.1.1",
		"typescript": "5.9.3"
	},
	"dependencies": {
		"react": "^19.1.0",
		"react-dom": "^19.1.0"
	}
}
